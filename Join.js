// Join method creates string from array by concatenating them
// has one parameter seperator and will be added between elements

const sports = ["Football", "Baseball", "Volleyball", "Hockey"];

const res = sports.join("-");

console.log(res); 